# Copiar para .env.local e preencher.
# .env.local é ignorado pelo git (ver .gitignore na raiz).

# URL do Web App publicado pelo Apps Script.
# Exemplo: https://script.google.com/macros/s/AKfycb.../exec
VITE_API_URL=

# Token compartilhado definido em backend/Code.gs (constante AUTH_TOKEN).
# Use um valor aleatório longo. Nunca commitar este arquivo.
VITE_API_TOKEN=
